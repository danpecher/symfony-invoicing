{% extends 'base.html.twig' %}
{% form_theme form 'form/fields.html.twig' %}

{% block body %}
    <h1 class="title">Nastavení uživatele</h1>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <div class="field is-grouped">
        <p class="control">
            <button class="button is-primary">Uložit</button>
        </p>
        <p class="control">
            <a href="{{ path('dashboard') }}" class="button is-link">Zrušit</a>
        </p>
    </div>
    {{ form_end(form) }}
{% endblock %}