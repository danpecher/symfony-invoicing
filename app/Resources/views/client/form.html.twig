{% form_theme form 'form/fields.html.twig' %}

{{ form_start(form) }}
{{ form_widget(form) }}
<div class="field is-grouped">
    <p class="control">
        <button class="button is-primary">Vytvořit</button>
    </p>
    <p class="control">
        <a href="{{ path('clients') }}" class="button is-link">Zrušit</a>
    </p>
</div>
{{ form_end(form) }}